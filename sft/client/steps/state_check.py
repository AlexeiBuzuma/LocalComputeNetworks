import logging
import time

from sft.common.config import Config
from sft.common.sessions.session_manager import SessionManager, SessionStatus

LOG = logging.getLogger(__name__)
_config = Config()


def client_state_check(data):
    # ToDo: Write client state check
    pass
